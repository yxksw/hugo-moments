{
  "name": "hugo-memos",
  "version": "1.0.0",
  "description": "Hugo Memos Blog",
  "scripts": {
    "build": "hugo && npx pagefind --site public --exclude-select '[data-pagefind-ignore]'"
  },
  "devDependencies": {
    "pagefind": "^1.1.0"
  }
}
