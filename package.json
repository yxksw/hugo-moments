{
  "name": "hugo-memos",
  "version": "1.0.0",
  "description": "Hugo Memos Blog",
  "scripts": {
    "build": "hugo && npx pagefind --site public --output-subdir pagefind"
  },
  "devDependencies": {
    "pagefind": "^1.1.0"
  }
}
