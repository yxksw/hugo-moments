# Pagefind 配置文件
# 只索引文章列表页，为每篇文章生成独立搜索结果

# 源目录
site: public

# 只包含文章列表页
include:
  - '/index.html'
  - '/posts/index.html'

# 排除其他页面
exclude:
  - '/tags/**'
  - '/search/**'
  - '/404.html'

# 为每个 data-pagefind-body 元素创建独立结果
collapse_content: false

# 添加锚点到结果
add_anchor_to_results: true
